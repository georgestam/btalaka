/ scrollToTop
/ ================

:sass
  .linked
      display: block
      position: relative
      top: -45px
      visibility: hidden


- content_for :title, I18n.t('views.pages.home.starfish_title')

.scrollToTop.circle
  %i.icon-up-open-big
= render 'home'
= render 'summary'
-# = render 'who_we_are'
-# = render 'courses'
-# = render 'media'
-# = render 'team'
-# = render 'Contact'
